{
    "name": "epixxcinema",
    "version": "1.0.0",
    "description": "epixx.ru test project",
    "main": "index.js",
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "start": "npm run build && node dist/index.js",
        "build": "babel index.js -d dist && babel app -d dist/app",
        "clean": "rm -rv dist"
    },
    "author": "Tim Marinin <marinintim@gmail.com>",
    "babel": {
        "presets": [
            "babel-preset-es2015"
        ]
    },
    "license": "ISC",
    "dependencies": {
        "ecstatic": "^1.4.0",
        "express": "^4.13.4",
        "hbs": "^4.0.0",
        "level": "^1.4.0",
        "lodash": "^4.12.0"
    },
    "devDependencies": {
        "babel-cli": "^6.8.0"
    }
}
